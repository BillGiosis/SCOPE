torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
tqdm>=4.65.0
pillow>=9.5.0
PyYAML>=6.0.0
requests>=2.31.0  # Required by official CLAD utilities
timm>=0.9.0  # Required for ResNet50D backbone
torch-pruning>=1.4.3  # Required for structural whole-model reallocation

# Note: the official CLAD package is loaded from the cladc_config.yaml file
